function [violationFreq, out_bounds, avg_cap] = nonviolationInteractive(pwr_case, pv_cap, irrad_time,...
    pct_load, dc_power, dc_ratio, dc_cap, p, options, dcBus, numBuses, pvBus, verbose)
    % Todo: 
end
